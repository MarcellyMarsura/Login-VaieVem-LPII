schema=CREATE SCHEMA USUARIO;
usuario=CREATE TABLE USUARIO.TB_USUARIO (ID_USUARIO INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), NOME VARCHAR(40) NOT NULL, EMAIL VARCHAR(40) NOT NULL UNIQUE, SENHA VARCHAR(40) NOT NULL, ATIVO BOOLEAN NOT NULL);